简单的伪分布式缓存
主线程监听客户端http请求, 通过线程启动从节点监听主节点发来的请求
使用从节点存储数据,数据结构为map[string]string, 每个从节点存储空间是groupName/key/value
主节点收到客户端请求后,通过其groupName定位到相应的从节点,然后主节点发送http请求到相应的从节点
从节点收到主节点的请求后取到数据并存储到map
缓存淘汰策略使用lru, 数据结构为双向链表, 每次存储一个键值对后检查缓存空间并进行释放